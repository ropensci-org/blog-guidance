on: pull_request
  
name: Render-Book-from-PR

jobs:
  bookdown:
    name: Render-Book
    runs-on: macOS-latest
    steps:
      - name: test
        run: |
          if  [[ ${NETLIFY_SITE_ID} != 0 ]]
          then
              npm install netlify-cli -g
          else
              echo "skipped for forks"
          fi
