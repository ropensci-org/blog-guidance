on: pull_request
  
name: Render-Book-from-PR

jobs:
  bookdown:
    name: Render-Book
    runs-on: macOS-latest
    steps:
      - name: test
        run: |
          fork=$(jq --raw-output .head.repo.fork "${GITHUB_EVENT_PATH}")
          echo "Fork is $fork"
